[package]
name = "caso-render"
description = "wgpu renderer for casoWASM: surface/grid/line passes, orbit camera, WGSL shaders"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
bytemuck = "1.25.0"
caso-kernel = { path = "../kernel" }
caso-meshing = { path = "../meshing" }
caso-surfaces = { path = "../surfaces" }
web-time = "1.1"
wgpu = "29"

[dev-dependencies]
pollster = "0.4"
